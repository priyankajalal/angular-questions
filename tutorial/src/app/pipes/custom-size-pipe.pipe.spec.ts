import { CustomSizePipePipe } from './custom-size-pipe.pipe';

describe('CustomSizePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomSizePipePipe();
    expect(pipe).toBeTruthy();
  });
});
