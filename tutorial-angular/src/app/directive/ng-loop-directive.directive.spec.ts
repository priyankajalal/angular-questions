import { NgLoopDirectiveDirective } from './ng-loop-directive.directive';

describe('NgLoopDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NgLoopDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
