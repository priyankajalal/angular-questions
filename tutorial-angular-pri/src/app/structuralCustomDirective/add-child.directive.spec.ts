import { AddChildDirective } from './add-child.directive';

describe('AddChildDirective', () => {
  it('should create an instance', () => {
    const directive = new AddChildDirective();
    expect(directive).toBeTruthy();
  });
});
